# pili++ 项目协作约定

## 项目目标

本仓库基于开源项目
[`bggRGjQaUbCoE/PiliPlus`](https://github.com/bggRGjQaUbCoE/PiliPlus)。
Android 端的核心改造目标是：用 AndroidX Media3 ExoPlayer 完整替代 mpv，
并保持 PiliPlus 原有播放器的全部用户可见功能、操作方式和业务闭环。

这里的“替代”不是“ExoPlayer 能把视频播出来”，而是：

- mpv 模式可用的功能，ExoPlayer 模式也必须可用；
- 相同入口、相同手势、相同控件应产生相同结果；
- 播放器状态必须正确回传到既有 Flutter UI 和业务逻辑；
- 功能不得因为切换到 ExoPlayer 而隐藏、静默失效或要求用户切回 mpv；
- Media3 没有直接对应能力时，应在原生桥接层或 Flutter 公共层实现等价结果；
- 在所有兼容项完成并经过真机验证前，不得宣称“mpv 已被完整替代”。

详细兼容清单见 `docs/android_exoplayer.md`，它与本文件共同构成验收标准。

## 产品名称

- 用户可见软件名固定为 `pili++`。
- Debug 构建可显示为 `pili++ debug`。
- 修改应用名称时，应同步检查 Android 主应用、Debug 变体、通知、媒体会话、
  画中画、分享面板和系统最近任务中的显示名称。
- 除非确有迁移必要，不因展示名称变化随意修改 Dart 包名、Android
  applicationId、Java/Kotlin package 或数据目录，避免破坏升级安装和用户数据。

## 最高优先级兼容原则

mpv 当前行为是 ExoPlayer 适配的兼容基线。以下做法不算完成：

- 使用 `if (!useExoPlayer)` 隐藏原有按钮、菜单、覆盖层或功能入口；
- ExoPlayer 分支留空、只打印日志、静默返回或显示“暂不支持”；
- 出错后无提示地回退 mpv，并把回退成功当作 ExoPlayer 已适配；
- 只实现原生播放，未接通 Flutter 控件、手势、状态和业务回调；
- 只通过编译或模拟器测试，就把功能标记为完成；
- 为修一个页面而破坏全屏、画中画、后台播放、直播或其他播放场景。

若某项暂时无法实现，应保留为明确的未完成项，记录原因、影响范围和下一步，
不能删除用户入口来掩盖缺口。

## 播放器架构边界

- 页面、业务逻辑、播放器控件、手势、弹幕、字幕和覆盖层保持为
  backend-neutral 的 Flutter 实现。
- mpv 与 ExoPlayer 通过统一播放器接口/适配层提供状态和操作，不让页面直接依赖
  某个后端。
- Android Media3 使用 Flutter `Texture` 接入现有播放器图层。
- 不使用会盖住 Flutter 控件的 `AndroidView`、原生 `PlayerView` 或独立原生控制层。
- 原生 Kotlin 桥接负责 Media3 会话、媒体源、音视频轨、Surface、生命周期和事件；
  Flutter 控制器负责把这些能力映射到现有 `PlPlayerController` 语义。
- 新代码不得继续扩大业务层对 mpv 对象、属性或命令的直接访问。
- 发现直接 mpv 依赖时，应迁移到公共接口，或把它隔离在 mpv adapter 内并记录债务。

## 必须闭环的功能范围

### 基础播放与状态

- 打开、播放、暂停、继续、停止、回放、循环和播完；
- 精确跳转、快进、后退、拖动进度条、断点续播；
- 时长、当前位置、缓冲位置、缓冲状态、播放速度和错误状态；
- 切清晰度/CDN/分P后保留位置、播放/暂停状态、倍速、音量和静音；
- DASH 音视频合流、音频模式、本地文件、UGC、PGC、课程、合集和互动视频；
- 直播最终也要由 ExoPlayer 闭环，不把“直播继续使用 mpv”视为最终结果。

### 控件与手势

- 单击视频区域必须能显示/隐藏完整控制层；
- 播放/暂停、快进、后退、进度条、时间、倍速、清晰度和全屏按钮可见且可用；
- 双击播放/暂停、长按倍速、横向滑动进度、纵向亮度/音量；
- 锁屏、旋转、横竖屏、全屏、折叠屏、手势冲突和返回键行为；
- 画面比例、适配方式、缩放以及水平/垂直翻转；
- 控件自动隐藏计时和用户操作后的恢复逻辑与 mpv 一致。

当前截图所反映的“点击视频区域没有任何控制按钮”属于 P0 阻断问题。
它未通过前，不得把点播控制层适配标为完成。

### 弹幕、字幕与进度增强

- 普通弹幕、高级弹幕、弹幕点击、发送弹幕和弹幕设置；
- B 站字幕、外部字幕、字幕轨选择、样式和拖动；
- 章节/看点、预览缩略图、高能进度条和 SponsorBlock；
- 上述图层必须与视频及控制层处在正确层级，触摸事件不能被 Texture 吞掉。

### 音频、系统与工具能力

- 音量、静音、音频焦点、后台音频、媒体通知和耳机/蓝牙控制；
- 音量均衡/响度归一化及音视频/字幕轨信息与选择；
- 画中画、前后台切换、息屏/亮屏、任务恢复和生命周期；
- 截图、动图截取、分享、保存、DLNA 等价闭环；
- 网络变化、CDN 重载、重试、解码失败和进程重建后的恢复。

“ExoPlayer 暂不支持截图”等提示只能作为迁移中的已知缺口，不能作为最终实现。

## 分批工作方式

每一批只处理一组可以独立验证的闭环，推荐顺序为：

1. P0：视频图层、点击控制层、播放/暂停、快进/后退、进度和状态同步；
2. P0：全屏、旋转、锁定、手势和画面适配；
3. P1：清晰度/CDN/分P切换及状态保持、异常恢复；
4. P1：弹幕、字幕、章节、预览、高能进度和 SponsorBlock；
5. P1：音频焦点、后台、媒体通知、画中画和生命周期；
6. P2：截图、动图、轨道选择、播放器信息、DLNA 和其他工具；
7. P2：直播、本地视频及剩余边缘场景；
8. 移除所有功能性 mpv 依赖、实验性文案和临时回退。

开始一批前先核对 mpv 的现有行为和所有调用点。完成一批后更新
`docs/android_exoplayer.md` 或对应的兼容记录，再进入下一批。

## 每批完成定义

一项功能只有同时满足以下条件才可标记完成：

1. ExoPlayer 具有与 mpv 等价的可见行为；
2. 控件、手势、状态流和业务副作用均已闭环；
3. mpv 路径未发生回归；
4. 已覆盖普通窗口、全屏以及适用的前后台/画中画场景；
5. `dart format`、Flutter 静态分析和相关测试通过；
6. Android Release 构建通过；
7. 在真实 Android 设备上与 mpv 模式逐项对照验证；
8. 没有新增“暂不支持”、隐藏入口、空实现或静默回退。

无法在当前环境完成真机验证时，必须明确写为“待真机验证”，不能写“已完成”。

## 排查与测试要求

- 修复前先定位事件链：Flutter 手势命中 → 控制层状态 → 控制器命令 →
  MethodChannel → Media3 → EventChannel → Flutter 状态刷新。
- 遇到“画面能播但控件无反应”，优先检查 Flutter 图层/命中测试、控制层显示条件、
  `Texture` 尺寸与遮挡、controller 初始化时序和事件订阅，不只检查 Media3 播放状态。
- 使用同一视频分别在 mpv 与 ExoPlayer 下执行相同动作并记录差异。
- 至少验证短视频、长视频、独立 DASH 音视频、切分P、切清晰度、播放完成和网络重试。
- 对异步生命周期操作防止重复创建、释放后回调、旧 session 污染新视频和
  MethodChannel 竞态。
- 不吞异常；错误应包含会话、媒体源阶段和 Media3 错误信息，同时给用户可理解反馈。

## 打包与签名

- 每批需要交付时，构建 Android Release APK；按用户需要提供通用包或分 ABI 包。
- 交付包必须来自完成验证的准确源代码状态，并记录版本、构建时间和 Git commit/diff。
- Release 包必须使用项目约定的同一套证书签名，保证后续可覆盖安装。
- 构建后使用 Android 签名校验工具确认 APK 已签名，并记录证书指纹。
- APK 文件名使用 `pili++`、版本号、ABI 和构建类型等可识别信息。
- keystore、`key.properties`、store password、key alias 和 key password
  属于敏感信息，不得写入本文件、提交 Git、粘贴到公开日志或打进 APK 旁的公开说明。
- 用户需要接管签名时，应把证书文件和四项签名信息通过明确的本地、未跟踪交付文件
  或用户指定的安全渠道一并交付；不能只给证书却遗漏密码、别名或别名密码。
- 在覆盖、替换或重新生成已有证书前必须确认；误换证书会导致已安装版本无法升级。

## 修改约束

- 工作区可能已有用户或前序批次的修改；不要重置、覆盖或顺手清理无关改动。
- 不使用 `git reset --hard`、`git checkout --` 等破坏性命令清理工作区。
- 修改尽量小而完整，避免把大规模重构和用户可见修复混在同一批。
- 不以更换第三方 UI 控件来绕过现有 PiliPlus 功能；优先复用原播放器 UI。
- 新增 Media3 依赖时检查版本兼容、APK 体积、混淆规则和最低 Android 版本。
- 除非用户明确要求，不修改上游仓库地址、许可证、应用 ID 或用户数据格式。

## 汇报规则

每批结束时用事实汇报：

- 本批闭环了哪些用户场景；
- 修改了哪些关键文件；
- 做了哪些自动化和真机验证；
- 生成包的完整路径、版本、ABI、签名状态和哈希；
- 仍有哪些 mpv 已有而 ExoPlayer 未闭环的项目；
- 下一批建议从哪个最高优先级缺口开始。

不要用“基本支持”“应该可以”“理论上没问题”替代测试结果。
