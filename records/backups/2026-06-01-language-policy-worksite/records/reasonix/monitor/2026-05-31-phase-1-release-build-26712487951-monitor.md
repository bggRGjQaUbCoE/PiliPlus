# Phase 1 Release Build 26712487951 Monitor — Signing Evidence

Date: 2026-05-31
Review owner: Codex
Monitor target: Build workflow_dispatch run `26712487951`
Head SHA: `a5d0d075cd80a35173355a52133057c7cec1679b`

Status: **FAILED** — candidate monitor evidence only. Does not close any release gate.

---

## 1. Reading Scope

Files and data consulted:

- `.github/workflows/build.yml` (at local HEAD `a5d0d075c` — same as run commit)
- `gh run view 26712487951` — run identity
- `gh run watch 26712487951` — live completion monitoring (~2m 20s)
- `gh api .../jobs` — job and step-level results
- `gh api .../artifacts` — artifact inventory
- `gh run view --log --job 78725089693` — full Release Android job log
- `git rev-parse HEAD` — local HEAD `a5d0d075c` matches run head SHA
- Prior monitor reports:
  - `records/reasonix/monitor/2026-05-31-phase-1-release-build-workflow-dispatch-monitor.md` (run `26680259984` at `80f5e6d6a`)
  - `records/reasonix/monitor/2026-05-31-phase-1-release-build-26711811133-monitor.md` (run `26711811133` at `a5d0d075c`)
- Codex reviews in `records/reasonix/review/`

---

## 2. Run Identity

| Field | Value |
|---|---|
| Run ID | `26712487951` |
| URL | https://github.com/CometDash77/PiliAvalon-Worksite/actions/runs/26712487951 |
| Event | `workflow_dispatch` |
| Branch | `phase-1-shielding-core` |
| Head SHA | `a5d0d075cd80a35173355a52133057c7cec1679b` |
| Workflow | Build |
| Status | `completed` |
| Conclusion | **`failure`** |
| Created | 2026-05-31T12:22:37Z |
| Updated | 2026-05-31T12:24:57Z |
| Duration | ~2m 20s |

---

## 3. Job Results

### Release Android — `failure`

| Step # | Step Name | Conclusion | Notes |
|---|---|---|---|
| 1 | Set up job | success | |
| 2 | 代码迁出 | success | |
| 3 | 构建Java环境 | success | |
| 4 | 安装Flutter | success | |
| 5 | Apply Patch | success | |
| **6** | **Write key** | **✅ success** | Secrets now configured! Hardened check passed. |
| 7 | Set and Extract version | success | Version: `2.0.7-a5d0d075c+5045` |
| **8** | **Flutter Build Release Apk** | **❌ failure** | `screen_brightness_android` Gradle `kotlin()` issue |
| 9 | Flutter Build Dev Apk | skipped | blocked |
| 10 | Rename | skipped | blocked |
| 11 | Capture Android signing fingerprints | skipped | blocked |
| 12 | Resolve release tag | skipped | blocked |
| 13 | Release | skipped | blocked |
| 14–16 | 上传 (3× APK) | skipped | blocked |
| 17 | 上传签名证据 | skipped | blocked |
| 32–35 | Post/Cleanup steps | success | |

### Other Jobs

| Job | Conclusion |
|---|---|
| linux_x64 | skipped |
| mac | skipped |
| win_x64 | skipped |
| ios | skipped |

### Failed Step — Full Error Log

From the `Flutter Build Release Apk` step (step 8):

```
Running Gradle task 'assembleRelease'...

FAILURE: Build failed with an exception.

Build file '/home/runner/.pub-cache/hosted/pub.dev/screen_brightness_android-2.1.5/android/build.gradle' line: 52

> Could not find method kotlin() for arguments
  [build_1om6w5yx8zt35whsjh8s2v3ln$_run_closure3@133f7d51]
  on project ':screen_brightness_android' of type org.gradle.api.Project.

> Get more help at https://help.gradle.org.

Gradle task assembleRelease failed with exit code 1
Error: Process completed with exit code 1.
```

**Root cause:** The `screen_brightness_android` Flutter plugin (v2.1.5) uses a `kotlin()` method in its `build.gradle` that is incompatible with the current Gradle/AGP version. This is the same issue previously observed in x86_64 dev APK builds (`Phase 1 CI`). This run confirms the issue **also affects release APK builds** — it is not limited to the dev/emulator path.

---

## 4. Release APK Artifact Inventory

**No artifacts were produced.** The build failed before any APK was created.

| ABI | Status |
|---|---|
| arm64-v8a | ❌ Not produced |
| armeabi-v7a | ❌ Not produced |
| x86_64 | ❌ Not produced |

---

## 5. Release / Pre-release Asset Inventory

**No release was created.** The `Resolve release tag` and `Release` steps were skipped due to the build failure.

---

## 6. Android_signing_evidence Artifact Status

**Not present.** The `Capture Android signing fingerprints` (step 11) and `上传签名证据` (step 17) steps were skipped.

However, this run confirms these steps **are present in the workflow** and would execute if the Flutter build succeeds. The workflow infrastructure for signing-evidence capture is correct.

---

## 7. apksigner verify --print-certs Output

**Not available.** No APKs were built.

---

## 8. Cover-install Requirements File Status

**Not present.** Would be generated by step 11 (`Capture Android signing fingerprints`) if build had succeeded.

---

## 9. Risks

| Risk | Severity | Detail |
|---|---|---|
| `screen_brightness_android` Gradle incompatibility | **Critical** | Previously thought to affect only x86_64 dev/emulator builds. This run proves it **also blocks release APK builds** on all ABIs. The `kotlin()` method used by `screen_brightness_android` v2.1.5 is incompatible with the current Gradle/AGP toolchain. |
| Blocking all release APK builds | **High** | Until the `screen_brightness_android` Gradle issue is resolved, no release APKs can be built via CI. This is no longer just a CI/emulator concern. |
| Previous APK run may be the only valid APKs | **Medium** | Run `26680259984` at `80f5e6d6a` produced APKs, but the signing status is unknown (soft-fail Write key). Those APKs may be the only ones available until the Gradle issue is fixed. |
| Workflow steps correct but unreachable | **Low** | The signing-evidence capture steps (11, 17) are correctly defined and would work — they just can't be reached until the Gradle issue is fixed. |
| Positive signal: secrets configured | **Positive** | The `Write key` step passed with `***` values for all four secrets. Signing secrets are now correctly configured in the GitHub repository. |

---

## 10. Unknowns

1. **Resolution path for `screen_brightness_android`.** Options include:
   - Upgrade `screen_brightness_android` to a version compatible with the current Gradle/AGP
   - Remove/replace the `screen_brightness_android` dependency if not needed for release builds
   - Patch the plugin's `build.gradle` to use the modern Kotlin plugin application syntax
   - The previous Reasonix implementer report proposed a Gradle workaround, but the user explicitly chose not to adopt it

2. **Whether run `26680259984` APKs are signed.** Those APKs were built with the soft-fail Write key pattern. If secrets were absent at that time, they may be unsigned or debug-signed.

3. **Impact scope of `screen_brightness_android`.** Is it a transitive dependency or a direct one? Can it be scoped to dev-only builds?

4. **Timeline for Gradle fix.** This is a user/product decision.

---

## 11. Verification Results

| Check | Result |
|---|---|
| Run identity confirmed | ✅ `26712487951`, workflow_dispatch, `a5d0d075c` |
| Run completed | ✅ `completed`, `failure` (2m 20s) |
| Write key step passed | ✅ All four secrets present (`***` redacted in log) |
| Flutter Build Release Apk failed | ✅ Confirmed — `screen_brightness_android` Gradle `kotlin()` issue |
| APK artifacts produced | ❌ None |
| Android_signing_evidence artifact | ❌ Not present |
| apksigner verify --print-certs | ❌ Not captured |
| Release created | ❌ None |
| Signing secrets configured | ✅ Confirmed (secrets resolve to non-empty values) |

---

## 12. Client/User Decision Needed

**Yes.** The following decisions are needed:

1. **Resolve the `screen_brightness_android` Gradle incompatibility.** This issue now demonstrably blocks release APK builds, not just dev/emulator builds. The `kotlin()` method in the plugin's `build.gradle` line 52 is incompatible with the current Gradle/AGP version.

2. **Decide on the approach:**
   - Accept a Gradle workaround (the Reasonix implementer proposal the user previously rejected)
   - Upgrade/replace `screen_brightness_android` dependency
   - Exclude `screen_brightness_android` from release builds if not needed
   - Other fix

3. **Verify run `26680259984` APKs locally.** If those APKs are signed and pass cover-install, they could be used for manual retest while the Gradle issue is being resolved.

4. **Re-trigger Build workflow_dispatch after the Gradle fix.** The signing-evidence pipeline (Write key → Build → Capture → Upload signing evidence) is structurally correct and ready to produce complete evidence once the Gradle issue is resolved.

5. **Release gates remain open:**
   - Release `Build` workflow_dispatch → **failed** (3rd attempt: `26680259984` produced APKs but no signing evidence; `26711811133` failed on secrets; `26712487951` failed on Gradle)
   - Release APK artifacts → **not produced** (this run)
   - `Android_signing_evidence` → **not produced**
   - `apksigner verify --print-certs` → **not captured**
   - Real-device cover-install → **pending**
   - User manual retest → **pending**
   - Phase 1 green/accepted/complete → **not closed**

---

*This artifact is candidate monitor evidence only. Release decisions require Codex review of this persisted artifact, a successful Build run after the Gradle fix, plus separate user cover-install and manual retest evidence.*
