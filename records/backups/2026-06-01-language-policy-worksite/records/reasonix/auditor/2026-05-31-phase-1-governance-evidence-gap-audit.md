# 治理证据缺口审计报告

**仓库:** CometDash77/PiliAvalon-Worksite
**分支:** phase-1-shielding-core（提示中提到 phase-1-shielding-acceptance-fixes，当前分支不同）
**审计者:** Reasonix Code（只读审计子代理）
**审计日期:** 2026-05-29 至 2026-06-01

---

## 1. 发现

### 1.1 两个命名的治理问题 — 缺失

对仓库中所有 1,584 个文件执行字符串搜索：

| 治理问题 ID | 搜索结果 |
|---|---|
| `phase-1-multi-agent-governance-gap` | **零匹配** |
| `phase-1-shielding-implementation-audit` | **零匹配** |

这些字符串在 `records/`、`.reasonix/`、根目录下的 `*.md`、`*.yaml` 或任何仓库文件中均未出现。

### 1.2 缺失的治理目录结构

| 目录 | 是否存在 |
|---|---|
| `.governance/` | **缺失** |
| `.reports/` | **缺失** |
| `.specs/` | **缺失** |

### 1.3 存在的证据（已盘点）

| 路径 | 内容 | 笔记 |
|---|---|---|
| `records/session/` | 42 个记录文件 | CI 历史、手动验收、故障报告、修复交接、发布笔记 |
| `records/reasonix/auditor/` | 6 个审计记录 | APK 范围、证据差距、4 个 Reasonix 主题检测（A/B/C/D） |
| `records/package-handoffs/` | 1 个交接文件 | 审计后重建；原始包负责人不可恢复 |
| `records/worksite-communications/` | 2 个文件 | Worker 提示 + Worker 1 分析 |
| `records/diffs/` | 1 个差异文件 | 选项 B 起始状态的快照 |
| `phase1-report.md`（根目录） | CI 摘要及证据清单 | 引用运行 26686823386，3/3 作业通过 |
| `.reasonix/evidence-check/` | 16 个运行时烟雾文件 | 引用 HEAD `7670673b0`，运行 26686823386 |
| `.reasonix/evidence-smoke/` | 16 个运行时烟雾文件 | 与 evidence-check 字节相同 |

### 1.4 证据复制（发现复用）

`.reasonix/evidence-check/` 和 `.reasonix/evidence-smoke/` 下的文件**完全相同**。两个目录都具有相同的 `screenshot-blankness.txt`（mean_luma=160.72）、相同的 `runtime-smoke-metadata.txt`（artifact_run_id=26686823386）和完全匹配的 16 文件证据集。

这可能是不良复制（出于存档目的的无害重复）或**不正确的证据复用**（同一套的两次标注）。无法通过只读工具确认原始来源档案。

### 1.5 CI 运行 — 失败与当前成功

**未发现**将旧失败 CI 运行复用为通过证据的情况。记录正确区分：

- **运行 26684680541**：打包失败，运行时烟雾被阻塞 — 标记为失败/被取代
- **运行 26686823386**：3/3 作业通过 — 基线成功
- **运行 26675065521**（白屏故障记录）：失败 — 被取代，未被作为通过证据复用
- **运行 26677982092** 和 **26679987266**（标签目标错误的预构建）：被取代

### 1.6 预构建链

| 预构建标签 | 状态 | 提交 |
|---|---|---|
| `phase-1-prebuild.26628409138` | 早期有效基线 | `cf9ed10f0` |
| `phase-1-prebuild.26675065521` | **失败**（白屏） | — |
| `phase-1-prebuild.26677982092` | **被取代**（标签定位错误） | `75eb67d33`（构建）→ `main`（标签） |
| `phase-1-prebuild.26679987266` | **被取代**（标签定位错误） | `5d0dfe673`（构建）→ `main`（标签） |
| `phase-1-prebuild.26680259984` | **当前有效基线** | `80f5e6d6a` |
| HEAD `7670673b0` | 无正式发布 APK | 向前 6 个提交（CI/文档/修复） |

---

## 2. 风险

### 2.1 命名治理问题的缺失
不存在提及 `phase-1-multi-agent-governance-gap` 或 `phase-1-shielding-implementation-audit` 的记录。这些关闭决策要么发生在外部（设计院/治理仓库是只读的且不在此审计范围内），要么从未被正式关闭。

### 2.2 运行时烟雾证据的复制
具有相同运行 ID（26686823386）和相同文件的完全相同文件夹的存在引发了关于证据溯源链的问题。

### 2.3 发布 APK 落后于 HEAD
有效的验收基线 `phase-1-prebuild.26680259984` 位于提交 `80f5e6d6a`，而 HEAD 是 `7670673b0`（向前 6 个提交）。HEAD 没有正式的发布 APK。

### 2.4 包重命名未完成
`pubspec.yaml name: PiliPlus`、`com.example.piliplus` applicationId、845 个 `package:PiliPlus/` 导入 — 均未更改。标记为黄色/债务，但没有记录正式豁免。

### 2.5 Reasonix 发现未解决
四个 Reasonix 审计（A/B/C/D，日期为 2026-06-01）发现：
- UP 屏蔽语义与甲方期望不匹配（keyword 匹配 authorName，无专用 `upName`/`upKeyword` 类型）
- 标签屏蔽因 API 负载限制在推荐源中无法工作（已提议 `tname` 备选方案但未实现）
- 旧 `RecommendFilter` 和新屏蔽同时作为 AND 门控运行（迁移分析存在但未应用）
- 设置页面是单页表单（甲方要求分类组织）

**这些是已识别的未解决问题，不是已解决的差距。**

---

## 3. 未知因素

| 未知因素 | 详细信息 |
|---|---|
| `phase-1-multi-agent-governance-gap` 是否已关闭？ | 此仓库中没有记录。在设计院仓库中，但不在本次审计范围内。 |
| `phase-1-shielding-implementation-audit` 是否已关闭？ | 此仓库中没有记录。不在本次审计范围内。 |
| GitHub API 速率限制验证 | Codex 无法独立验证运行 26686823386 在 GitHub 上的状态（HTTP 403 API 速率限制）。 |
| HEAD 当前的 CI 状态 | HEAD `ce5f6915d` 仅更改了记录，未触发新的 CI。 |
| 设计院治理仓库内容 | 引用了但无法访问。治理记录、审核轨迹和技术主管签核可能存在于外部。 |

---

## 4. 验证结果

### 4.1 证据类别 — 存在/缺失矩阵

| 证据类别 | 状态 | 定位 |
|---|---|---|
| **包所有权文档** | ⚠️ 存在但受限 | 审计后重建；原始包负责人不可恢复 |
| **会话交接记录** | ✅ 存在 | `records/session/` 中的 42 个文件 |
| **CI/运行 URL** | ✅ 存在 | 多个运行 URL 已记录 |
| **技术主管审核证据** | ❌ **缺失** | 无独立签核文件 |
| **运行时烟雾测试证据** | ✅ 存在 | `.reasonix/evidence-check/` — 16/16 文件 |
| **发布说明** | ✅ 存在 | `phase1-report.md` |
| **字段差异关闭矩阵** | ❌ **缺失** | 无文档系统跟踪设计差异 |

### 4.2 甲方验收状态（2026-05-31）

| 项目 | 甲方结果 | 状态 |
|---|---|---|
| 启动/无白屏 | 通过 | ✅ 通过 |
| UP 屏蔽语义 | 不满意（关键词匹配范围过广） | ❌ 未通过 |
| 长按卡片 UX | 不够美观（缺少封面预览） | ❌ 未通过 |
| 相关视频屏蔽 | 有效 | ✅ 通过 |
| 推荐源中的标签屏蔽 | 无效（API 不提供标签） | ❌ 未通过 |
| 上游屏蔽重复 | 两个系统都处于激活状态 | ❌ 未通过 |
| 设置页面分类 | 需要分类整理 | ❌ 未通过 |
| 全局开关 | 有效 | ✅ 通过 |
| 重启持久性 | 有效 | ✅ 通过 |

### 4.3 仍为黄色/红色的门禁

| 门禁 | 状态 |
|---|---|
| CI 运行 26686823386 | ✅ 绿色 |
| 运行时烟雾证据 | ✅ 绿色（16/16 文件） |
| 手动验收 | 🟡 黄色 — 5 个未关闭项 |
| 技术主管审查 | 🟡 黄色 — 无独立签核 |
| 甲方验收 | 🟡 黄色 — 等待被接受的修复 |
| 包重命名（package-runtime-contract） | 🟡 黄色 — 未改变；债务 |
| 封面安装 | 🟡 黄色 — 未由 CI 证明 |

---

## 5. 需要客户决定的事项

1. **`phase-1-multi-agent-governance-gap` 和 `phase-1-shielding-implementation-audit` 的关闭状态。** 此仓库中没有包含或关闭它们的记录。它们是否被外部跟踪？

2. **证据复制处理。** `.reasonix/` 下的两个证据目录相同。应删除重复目录或重新映射以消除歧义。

3. **包重命名豁免。** 包重命名已标记为债务并排除在 Phase 1 范围之外。此豁免是否已正式接受？

4. **设计院审核轨迹。** 治理记录、技术主管审查和 `phase-1-*` 问题关闭可能存在于外部设计院治理仓库中。

5. **五个未解决的甲方问题**需要优先级决定：哪些是 Phase-1 阻断器，哪些可以降级为债务/后续阶段？

6. **验收基线模糊性。** 有效的验收预构建来自提交 `80f5e6d6a`，比 HEAD 落后 6 个提交。客户应确认此基线对手动验收是否可接受。

---

**⚠️ 本报告是只读候选审计。不宣布任何 gate 关闭、通过或绿色。**
